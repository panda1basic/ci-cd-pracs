include:
  - local: pipelines/templates/bases.yml

deploy:
  tags:
    - builder
  extends:
    - .base
    - .base-pr4
  script:
    - mkdir -p ~/.ssh
    - echo "$SSH_PRIVATE_KEY" > ~/.ssh/id_rsa
    - chmod -R 700 ~/.ssh
    - >
      ssh -o StrictHostKeyChecking=no -i ~/.ssh/id_rsa deployer@89.169.1.105
      "sed -i.bak '/burmatovdd\/flask-app/ c\PROD_IMAGE-$APP_DIR=burmatovdd/$APP_DIR:$CI_COMMIT_SHA' /home/deployer/src/pr4/.env"
