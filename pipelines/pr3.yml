variables:
  PROJECT_PATH: src/pr3

.base:
  tags:
    - security

pr3:build:
  stage: build
  image: docker:latest
  script:
   - docker build $PROJECT_PATH/docker-compose.yml -t burmatovdd:flask-app:$CI_COMMIT_SHA
   - docker push burmatovdd:flask-app:$CI_COMMIT_SHA
